# Gift Surprise Web App 🎁

A simple full-stack web application using:

- HTML, CSS, JavaScript
- Flask (Python)
- MySQL

Users click numbers (1–10) to reveal hidden gifts stored in a database.

## How to Run
1. Install Flask and MySQL connector
2. Run `python app.py`
3. Open `http://127.0.0.1:5000`
